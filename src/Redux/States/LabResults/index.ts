import {Actions, Reducer} from './LabResultsSlice';
import * as Selectors from './LabResultsSelector';
import * as Thunks from './LabResultsThunk';

const LabResultsState = {
  Actions,
  Selectors,
  Thunks,
};

export {Reducer};

export default LabResultsState;
