import {Actions, Reducer} from './GeneralSlice';
import * as Selectors from './GeneralSelector';
import * as Thunks from './GeneralThunk';

const GeneralState = {
  Actions,
  Selectors,
  Thunks,
};

export {Reducer};

export default GeneralState;
